{
  "name": "ordenanzas-hcd-site",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "test": "vitest run",
    "check": "astro check && tsc --noEmit",
    "build": "astro build",
    "build:search": "pagefind --site dist"
  },
  "dependencies": {
    "astro": "^4.16.0",
    "@astrojs/sitemap": "^3.2.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "pagefind": "^1.1.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
